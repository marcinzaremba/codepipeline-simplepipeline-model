# Replace with values of your choice:
# bucket_name = <your-bucket-name>
# stack_name = <your-desired-stack-name>
current_dir = $(shell basename $$(pwd))
bucket_name = $(current_dir)
stack_name = $(current_dir)